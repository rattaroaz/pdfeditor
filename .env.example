# Frontend (Vite) — no secrets in client bundle
VITE_LOG_LEVEL=info
# VITE_ENABLE_LOG_VIEWER=true

# Playwright E2E (set automatically by scripts/dev-e2e.mjs)
# VITE_E2E=true

# Rust tracing filter (used when running tauri dev / release binary)
RUST_LOG=info,pdfeditor=debug
